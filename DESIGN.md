# DESIGN — 나또감 (가제)

> 처음엔 석재 표본집이었지만, 지금은 전 화면이 배틀카드다.
> 사진 대신 등급별 색과 캐릭터 일러스트, 숫자, 글자로 짓는 화면.

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.2 — 배틀카드 팔레트를 카드리스트 전용에서 랜딩·랭킹까지 전 화면으로 확장, 배경을 베이지로 개정 |
| 작성일 | 2026-08-14 (최초) / 2026-08-24 (배틀카드 개정, 전 화면 확장) |
| 관련 문서 | PRD.md |
| 플랫폼 | **웹** — 모바일 브라우저 기준, 라이트 모드 고정. Android 네이티브는 이후 |
| 단위 | `px` 기준. 이 문서의 모든 치수는 CSS 픽셀이다 |

---

## 0. 배틀카드 개정 안내 (v1.2)

최초 설계는 "석재 표본집" 컨셉으로, 카드 등급을 대리석/화강암/사암/점토 **재질**로 구분했다. 실제 구현 과정에서 방향을 **배틀카드 컨셉**으로 바꿨고, 처음엔 카드리스트("내 카드")에만 적용했다가 이후 **랜딩페이지와 랭킹페이지까지 확장**했다. 재질 시스템(§3)은 이제 어디에도 쓰이지 않는 과거 기록이다.

- 재질(질감) 대신 **등급별 색 팔레트**로 등급을 구분한다 (§2.3) — `card5`(카드리스트) · `front`(랜딩 3D 쇼케이스 카드) · `rank-podium-item`/`rank-row`(랭킹)가 `.g-s/.g-a/.g-b/.g-c/.g-u` 팔레트 정의 하나를 공유한다.
- 카드 우상단(또는 이미지 칸)에 **등급별 캐릭터 일러스트**(돼지 마스코트)를 쓴다 — 카드리스트(§6.2b)와 랜딩 쇼케이스 카드. 랭킹은 아바타 원 안 모노그램을 유지하되 등급색으로 물들인다.
- 인생맛집 카드에는 **상시 홀로그램 효과**(오로라+프리즘, 카드리스트는 크리스탈 글레어까지)가 돈다 (§6.2c) — §1.2의 "모션은 등급 전환 하나뿐" 원칙의 명시적 예외다.
- 방문 횟수와 누적 금액을 **항상 함께**, 카드리스트는 카드 하단 중앙에 능력치처럼 크게 보여준다 — 정렬 기준에 따라 하나만 보여주는 §5.3의 원래 계획은 폐기됐다.
- 모서리 반경 4px(각짐) 대신 10px(둥근 배틀카드 모서리)를 쓴다.
- **배경을 흰색이 아닌 석회암 판(회색 계열)에서 따뜻한 베이지(`#E7DCC3`)로 바꿨다** — 전 페이지 공통, CSS 변수명도 `--limestone` → `--beige`로 바꿨다 (§2.1).
- 카드리스트에 **이름 검색**을 추가했다 (§6.5) — 등급 구역 안에서 즉시 필터링.

## 1. 디자인 원칙

### 1.1 전제

PRD가 이미 디자인을 강하게 규정한다.

- **실사 사진은 없다.** 화면을 채우는 것은 색, 캐릭터 일러스트, 숫자, 글자다. (v1.0에서는 "재질, 숫자, 글자뿐"이었으나 카드리스트가 색+캐릭터 일러스트로 개정됨 — §0)
- **첫 화면이 통계다.** 숫자 가독성이 최우선이다.
- **대표 숫자는 인생맛집 개수다.** 이 서비스는 가계부가 아니라 컬렉션이다.

### 1.2 원칙

카드리스트는 배틀카드로 개정되며(§0) 아래 표도 함께 바뀌었다. 랜딩페이지 쇼케이스 카드에는 여전히 원래 취지(절제·재질)가 적용된다.

| # | 원칙 | 의미 |
|---|---|---|
| 1 | 구조는 도감, 표현은 카드마다 | 카드와 격자라는 수집의 뼈대는 쓰되, 카드리스트는 등급별 색과 캐릭터 일러스트로 뚜렷하게 표현한다 — 광물 표본집보다 포켓몬/배틀카드 도감에 가깝다. |
| 2 | 등급은 색으로 뚜렷하게 | 등급마다 카드 프레임·배경·배지 색이 통째로 바뀐다. 절제보다 한눈에 구분되는 대비를 우선한다. |
| 3 | 상시 모션은 인생맛집 한 곳에만 | 카드리스트에서 상시 루프 애니메이션은 인생맛집 카드의 홀로그램(§6.2c)뿐이다. 등급 전환 모션(§7.2)은 별도이며 아직 미구현이다. |
| 4 | 색은 항상 활자와 함께 | 색만으로 등급을 전달하지 않는다. 등급 배지 글자를 항상 병기한다. |
| 5 | 강등은 축하하지 않는다 | 승격과 강등의 연출은 대칭이 아니다. (§7.2, 미구현) |

### 1.3 시그니처

**등급 변경 시 카드 색이 실제로 바뀌는 전환.** (v1.0에서는 "재질이 바뀌는 전환"이었으나 재질 자체가 카드리스트에서 폐기됨 — §0)

이 서비스에서 기억에 남을 단 하나의 순간이다. 색이 장식이 아니라 상태 그 자체임을 증명하는 지점이며, 여기에 모든 모션 예산을 쓴다. **미구현** — §7.2에 계획만 있다.

---

## 2. 색

### 2.1 팔레트

바탕은 흰색이 아니다. CSS 변수는 `--beige`(2026-08-24 이전엔 `--limestone`이었다). 이 팔레트는 헤더·배경·버튼 등 **전역 UI**에 쓴다. 카드 자체(카드리스트·랜딩·랭킹)는 §2.3의 별도 팔레트를 쓴다.

| 이름 | HEX | 용도 |
|---|---|---|
| 베이지 | `#E7DCC3` | 배경. **따뜻한 톤으로 개정**(2026-08-24) — 이전엔 `#DAD5CC`(석회암, 회색 계열)였다 |
| 먹 | `#2A2724` | 본문, 식당명, 숫자 |
| 재 | `#6E675E` | 보조 정보, 라벨 |
| 무광 황동 | `#A8874E` | 강조 면, 아이콘, 기록 버튼 |
| 다크 브론즈 | `#6B5327` | 강조 글자 |

### 2.2 황동 사용 규칙

금색은 가장 쉽게 촌스러워지는 색이다. 다음을 엄격히 지킨다.

- **화면당 황동은 한두 곳까지.** 남발하면 위계가 죽는다.
- 그라디언트 금색 금지. 무광 단색만.
- 반짝임, 광택, 메탈릭 효과, 금색 테두리 금지.
- 황동은 **면과 아이콘**에만. 글자에 색이 필요하면 다크 브론즈를 쓴다. 밝은 배경에서 황동 글자는 대비가 부족하다.

> 카드리스트의 인생맛집 카드 프레임(`#C99A3D`)은 이 무광 황동(`#A8874E`)과 다른 값이며, §2.3 배틀카드 팔레트 소속이다. 화면당 황동 예산에 포함하지 않는다 — 인생맛집 카드가 여러 장 나열돼도 브론즈/황동을 반복 소모하는 게 아니다.

### 2.3 등급 팔레트 (배틀카드)

재질(§3, 이제 어디에도 쓰이지 않음) 대신 **등급별 색 팔레트**로 구분한다. CSS는 `.g-s/.g-a/.g-b/.g-c/.g-u` 클래스 하나로 정의하고, 카드리스트(`.card5`) · 랜딩 3D 쇼케이스 카드(`.front`) · 랭킹 포디움/리스트(`.rank-podium-item`/`.rank-row`)가 이 정의 하나를 그대로 공유한다(`assets/styles.css` 최상단). 등급마다 프레임(테두리) · 페이스(카드 배경 그라디언트) · 배지 플레이트 색이 통째로 바뀐다. 그라디언트는 상단이 밝고 하단이 짙은 165° 두 톤이다.

| 등급 | 프레임 | 페이스(밝음→짙음) | 배지 플레이트 | 배지 글자 |
|---|---|---|---|---|
| 인생맛집 | `#C99A3D` | `#FFFCF3` → `#F6DFA0` | `#8B6B1F` | `#FFF6E0` |
| 맛집 | `#5E9E4C` | `#F3FBEE` → `#C7E8B4` | `#2F5F27` | `#EAFBE3` |
| 무난 | `#4B8FC4` | `#EFF7FD` → `#B9DCF3` | `#245A83` | `#E7F4FC` |
| 비추 | `#8A61B0` | `#F7F0FA` → `#DAC2E8` | `#4E3170` | `#F3E9F8` |
| 미지정 | 없음(점선) | `#D8D3CA` (석회암보다 살짝 어두움) | 없음 | `#6E675E`(재) |

- 카드 본문 글자는 등급 불문 **먹색**(`#2A2724`)을 쓴다. 배지 플레이트 위 글자만 위 표의 밝은 색을 쓴다.
- 등급 배지 글자 좌우에 작은 다이아 핀(`currentColor`, 3.5px, 회전 45°)을 둔다 — 미지정에는 없다.
- 등급 구역 라벨(§5.2 board-zone-label)도 같은 계열의 짙은 색으로 물들인다: 인생맛집 `#8B6B1F`, 맛집 `#2F5F27`, 무난 `#245A83`, 비추 `#4E3170`.
- 카드 모서리 반경은 10px(§5.2) — 카드리스트·랜딩 쇼케이스 카드 공통. §3.1의 "석재는 둥글지 않다"는 이제 어디에도 적용되지 않는다.

---

## 3. 재질 시스템 (폐기 — 과거 기록)

**이 절은 더 이상 어디에도 쓰이지 않는다.** 처음엔 카드리스트가, 이후엔 랜딩페이지의 3D 쇼케이스 카드까지 재질 대신 §2.3의 색 팔레트로 옮겨갔다(2026-08-24). 아래는 "석재 표본집" 최초 설계를 남겨둔 기록이며, 실제 재질 텍스처 CSS(`--m-vein` 등 SVG feTurbulence 변수)도 코드에서 지웠다.

### 3.1 등급별 재질

위로 갈수록 결이 정교해지고, 아래로 갈수록 배경에 가라앉는다. 재질의 영속성이 등급의 위계다.

| 등급 | 재질 | 표면색 | 결 | 결 색 | 그림자 |
|---|---|---|---|---|---|
| 인생맛집 | 대리석 | `#FAF8F5` | 가늘고 유려한 맥 | `#CFC8BC` | 깊음 (y+6, blur 16, 12%) |
| 맛집 | 화강암 | `#EBE7E0` | 촘촘한 미세 반점 | `#B4AEA4` | 중간 (y+3, blur 8, 10%) |
| 무난 | 사암 | `#DFD8CA` | 거칠고 균일한 입자 | `#BCB0A0` | 얕음 (y+1, blur 4, 8%) |
| 비추 | 점토 | `#D3CEC6` | 결 없음 | — | 없음 |
| 미지정 | 무지 | `#D8D3CA` | 없음, 점선 테두리 | — | 없음 |

점토(`#D3CEC6`)는 배경(`#DAD5CC`)보다 미세하게 어둡다. 떠오르지 않고 가라앉는다.

### 3.2 구현 규칙

- 텍스처는 **등급당 이미지 1장씩만** 만들어 전 카드가 재사용한다. 카드마다 생성하면 목록 스크롤이 끊긴다.
- 노이즈 강도는 알파 8% 이하. 원본 표면색과의 명도차 5% 이내.
- 다크 모드는 v1 범위 밖. 지원 시 석재 팔레트 전체를 재설계해야 하며, 단순 반전은 불가능하다.

### 3.3 대비 검증 (필수)

점토 카드 위 먹색 글자가 가장 위험하다. 구현 시 실측하고 WCAG AA(4.5:1) 미달이면 **점토 톤을 밝히는 쪽으로** 조정한다. 글자를 어둡게 하는 방향은 이미 한계다.

---

## 4. 타이포그래피

### 4.1 서체

전부 산세리프. Pretendard 단일 계열을 쓰되, **굵기와 크기의 대비를 극단적으로 벌려** 위계를 만든다. 세리프를 쓰지 않기로 한 이상 도록 같은 무게는 서체가 아니라 여백·재질·황동이 짊어진다.

**웹에서의 조달**: CDN(jsDelivr)의 동적 서브셋 버전을 쓴다. 한글 전체 자소를 받으면 용량이 커지므로 실제 쓰는 글자만 내려온다. CDN이 죽어도 화면이 무너지면 안 되므로 폴백을 반드시 지정한다.

```css
font-family: 'Pretendard Variable', Pretendard,
             -apple-system, system-ui, 'Malgun Gothic', sans-serif;
```

### 4.2 역할

크기 단위는 `px`.

| 역할 | 굵기 | 크기 | 자간 | 용도 |
|---|---|---|---|---|
| 대표 숫자 | ExtraBold (800) | 72 | -3% | 대시보드 인생맛집 개수 |
| 지표 숫자 | Bold (700) | 28 | -1% | 누적 금액, 방문 횟수 |
| 식당명 | SemiBold (600) | 16 | 0 | 카드, 목록 |
| 본문 | Regular (400) | 15 | 0 | 메모, 설명 |
| 라벨 | Medium (500) | 12 | +2% | 보조 정보, 단위 |

대표 숫자는 좁은 화면에서 넘치므로 `clamp()`로 하한을 둔다. 폰 세로 기준 최소 48px.

### 4.3 숫자 규칙

**등폭 숫자(tabular figures)를 반드시 켠다.** 금액과 횟수가 세로로 나열되는 화면이 많은데, 비례 숫자를 쓰면 자릿수가 어긋나 지저분해 보인다.

```
font-feature-settings: "tnum" 1;
```

- 금액은 만원 단위 축약: `380만원`, `1,240만원`
- 방문 횟수는 항상 `회` 단위 병기
- 단위는 숫자보다 한 단계 작고 재색

---

## 5. 레이아웃

### 5.1 간격

4의 배수. 기본 단위 4px. CSS 커스텀 속성으로 토큰화한다 (`--xs` ~ `--xl`).

| 토큰 | 값 | 용도 |
|---|---|---|
| xs | 4 | 활자 간 |
| sm | 8 | 카드 내부 요소 간 |
| md | 16 | 카드 내부 여백, 격자 간격 |
| lg | 24 | 화면 좌우 여백 |
| xl | 40 | 섹션 구분 |

### 5.2 식당 격자

**등급 구역(board-zone)으로 나눈 도감판.** 인생맛집→맛집→무난→비추→미지정 순서로 구역을 위에서 아래로 두고, 구역마다 자체 `auto-fill` 격자를 쓴다. 열 수는 화면 폭과 구역별 카드 최소폭(180/165/150/140/140px)에 따라 유동적이다 — 고정 2열이 아니다.

- 카드 비율 **3:4** (세로로 긴 직사각형). 긴 한글 식당명을 두 줄로 받는다.
- 모서리 반경 **10px**. 배틀카드 팔레트(§2.3)로 개정되며 각진 모서리("석재는 둥글지 않다") 규칙은 전 화면에서 폐기됐다 — 랜딩 쇼케이스 카드도 같은 10px를 쓴다(2026-08-24 확장, §0).

카드에는 **네 가지**를 넣는다.

```
┌──────────────┐
│ 을지로   🐷  │  ← 좌: 식당명 최대 2줄, 말줄임 / 우: 등급별 캐릭터(§6.2b)
│ 골뱅이       │
│ ── 맛집 ──   │  ← 등급 배지 플레이트, 등급색 배경 + 흰 글자 (§6.2)
│  18   ·  96  │  ← 방문·누적을 능력치처럼 항상 함께, 카드 하단 중앙 (§5.3)
│  방문   만원 │
└──────────────┘
```

### 5.3 방문·누적 표시

방문 횟수와 누적 금액을 **항상 함께**, 카드 하단 중앙에 세로 구분선으로 나눠 크게 보여준다(게임 카드의 능력치 표기 방식). ~~정렬 기준에 따라 숫자 하나만 바꿔 보여준다~~는 v1.0의 계획이었으나 구현되지 않았고, 카드리스트에는 애초에 정렬 선택 UI가 없다 — 각 등급 구역 안에서 방문 횟수 내림차순(동률 시 누적 금액 내림차순)으로만 정렬된다.

---

## 6. 컴포넌트

### 6.1 대시보드 대표 숫자

화면 상단, 좌측 정렬. 가운데 정렬하지 않는다 — 가운데 정렬한 큰 숫자와 작은 라벨은 어디에나 있는 기본값이다.

```
인생맛집
8 / 9              ← 72pt ExtraBold, 우측 정원은 재색 절반 크기
멀리서 일부러라도 또 가는 곳
```

정원 대비 표기(`8 / 9`)가 희소성을 상시 노출한다. PRD상 v1은 초과를 차단하지 않으므로 초과 시에는 `11 / 9`로 표시하고 숫자를 다크 브론즈로 물들인다. 경고문은 띄우지 않는다. 숫자가 이미 말하고 있다.

### 6.2 등급 배지 (카드리스트 개정판)

v1.0에서는 카드 우상단에 활자만(도형 배경 없음) 두는 안이었다. 실제로는 §2.3 배틀카드 팔레트로 바뀌며 아래와 같이 개정됐다:

- 카드 **가운데 가로 전체 폭의 배지 플레이트**(band) — 식당명/이미지 행과 방문·누적 행 사이. 우상단 배치가 아니다.
- **도형 배경 있음.** 플레이트 배경은 등급색(§2.3의 "배지 플레이트" 열), 글자는 같은 표의 "배지 글자"(밝은 크림/화이트 계열) 색.
- 11.5px Bold. 좌우에 작은 다이아 핀(현재색, 회전 45°)을 둔다.
- **인생맛집만 다크 브론즈**였던 규칙은 폐기 — 이제 네 등급 모두 각자의 플레이트 색 위에 밝은 글자를 쓴다.
- 미지정은 플레이트 배경 없이 재색 글자만, 다이아 핀도 없다.
- 네 글자(`인생맛집`) 줄임말 전환(§10 미결정)은 아직 적용하지 않았다 — 플레이트가 카드 전체 폭이라 식당명과 공간을 다투지 않는다.

### 6.2b 등급 캐릭터 이미지

v1.0에서는 카드 우상단 이미지 칸을 "지금은 비워둔다"로 남겨뒀다. 실제로는 등급별 돼지 캐릭터 일러스트로 채웠다.

- 위치: 카드 우상단, 식당명과 같은 행의 오른쪽 절반 (`grid-area: image`)
- 등급별로 다른 동작의 같은 캐릭터: **인생맛집** 배부르게 뻗어 엄지 척 · **맛집** 그릇째 파묻혀 먹는 중 · **무난** 숟가락으로 한 입 뜨는 중 · **비추** 손대지 않고 음식만 바라봄
- 배경을 따로 두지 않는다 — 이미지 자체를 배경 투명 PNG로 오려내 카드의 등급색이 캐릭터 주변에 그대로 비친다
- 미지정은 캐릭터 없이 빈 칸 그대로 (아직 등급이 없다는 뜻)
- 에셋 경로: `assets/img/pig-s.png` / `pig-a.png` / `pig-b.png` / `pig-c.png` — 등급당 이미지 1장씩 재사용(§3.2와 동일한 재사용 원칙)

### 6.2c 인생맛집 홀로그램

인생맛집 카드에는 상시 루프 애니메이션 3겹이 얹힌다 — 카드리스트에서 유일하게 상시 도는 모션이며(§1.2 원칙 3), `prefers-reduced-motion: reduce`에서는 셋 다 정지한다.

| 레이어 | 효과 | 주기 | 블렌드 모드 |
|---|---|---|---|
| 오로라 | 초록·청록·보라·핑크 방사형 광휘가 서서히 떠오르듯 움직임 | 13초 | soft-light |
| 프리즘 홀로 시트 | 무지개색 대각선 줄무늬가 흐름 | 8초 | soft-light |
| 크리스탈 글레어 | 고정된 결면 잔선 위로 밝은 빛줄기가 대각선으로 훑고 지나감 | 5초 | screen |

카드 자체 색(프레임·페이스)은 다른 등급과 같은 방식으로 만든 골드 톤이다 — 이 세 레이어만이 인생맛집을 구분 짓는다.

### 6.3 기록 버튼

- 전역 고정, 우하단
- 무광 황동 원형, 지름 56px
- `position: fixed`. 모바일 브라우저 하단 UI에 가리지 않도록 `bottom: calc(16px + env(safe-area-inset-bottom))`
- 아이콘은 흰색 선 아이콘 하나
- **화면당 황동 한두 곳 규칙에 따라, 기록 버튼이 있는 화면에서는 다른 황동 요소를 쓰지 않는다.**

### 6.4 빈 상태

PRD의 단계적 공개를 따른다. 빈 화면은 사과하지 않고 행동을 지시한다.

| 기록 수 | 문구 |
|---|---|
| 0건 | 첫 기록을 남기세요 / 세 곳을 기록하면 통계가 열립니다 |
| 1~2건 | 두 곳 더 기록하면 통계가 열립니다 |

### 6.5 카드리스트 검색 (2026-08-24 신규)

담은 맛집이 늘어나면 등급 구역을 스크롤해서 찾기보다 이름으로 바로 찾는 편이 빠르다.

- 미지정 안내 배너(§6.2 위 문단) 자리에, 카드가 하나라도 있으면 항상 검색창을 띄운다
- 입력할 때마다 즉시 필터링(별도 버튼·디바운스 없음) — 이름에 검색어가 포함된 곳만 남긴다
- 등급 구역 구조(§5.2)는 그대로 유지한다. 매치가 하나도 없는 구역은 통째로 숨기고, "아직 등록한 맛집이 없습니다"(진짜 빈 구역용 문구)는 검색 결과 없음과 구분해 "'…'과 일치하는 맛집이 없습니다"를 쓴다
- 검색 중에는 미지정 안내 배너를 숨긴다 — 검색으로 걸러진 화면에 안 보이는 가게로 유도하는 배너를 얹지 않는다
- 정렬(등급 tier → 방문 내림차순 → 누적 내림차순)은 검색 여부와 무관하게 동일하다

### 6.6 랭킹 페이지 카드 (2026-08-24 신규)

랭킹(`rank.html`)의 포디움 카드와 4위 이하 리스트 행도 §2.3 등급 팔레트를 쓴다.

- 포디움 카드: 배경을 등급 페이스 그라디언트로, 테두리를 등급 프레임 색으로. 아바타 원은 반투명 흰색(카드 자체가 이미 색이 있어서 아바타까지 물들이면 과하다)
- 등급 pill 배지: 배경을 등급 배지 플레이트 색, 글자를 밝은 배지 글자색으로 — §6.2와 같은 방식
- 4위 이하 리스트 행: 왼쪽에 3px 등급색 테두리(accent bar)만 추가 — 행 전체를 카드처럼 채우진 않는다. 작은 아바타(`rank-avatar-sm`)는 등급 페이스의 짙은 톤으로 살짝 물들인다
- **순위 색은 그대로 등급과 분리한다** — 1위 브론즈 링, 2·3위 재색 링은 유지. 등수는 등급 정보가 아니다(§rank.js 상단 주석)
- 미지정 등급 항목은 점선 테두리로, 캐릭터 이미지는 쓰지 않는다(§6.2b와 동일 원칙)

---

## 7. 모션

### 7.1 원칙

서비스에서 큰 모션은 **등급 전환 하나뿐**이었으나, 카드리스트 개정(§0)으로 **인생맛집 카드의 상시 홀로그램 루프(§6.2c)**가 두 번째 예외로 추가됐다. 나머지 화면 전환은 표준 페이드, 200ms 이하. 여기저기 움직이면 아무것도 특별하지 않다는 원칙은 유지한다.

단, 랜딩페이지는 예외다 (§10 미결정 #7). 여기서 말하는 절제는 서비스 내부 화면의 규칙이다.

### 7.2 등급 전환 (미구현)

아래는 계획이며, 실제 등급 변경(`assets/cardlist.js`의 `setGrade()`)은 현재 텍스트/색만 즉시 갱신하고 이 전환 애니메이션은 아직 만들지 않았다.

| 방향 | 시간 | 이징 | 부가 |
|---|---|---|---|
| 승격 | 500ms | ease-out | 햅틱 1회, 그림자 심화 동반 |
| 강등 | 250ms | linear | 햅틱 없음, 소리 없음 |

- 구현은 **카드 색 크로스페이드**(v1.0에서는 "텍스처 크로스페이드"였으나 재질이 색으로 바뀜에 따라 개정 — §0). 파티클, 광채, 스케일 튐 없음
- 승격은 색이 짙어지며 카드가 떠오르고, 강등은 같은 전환을 되감으며 가라앉는다
- 시스템 모션 줄이기 설정이 켜져 있으면 **즉시 전환** — `@media (prefers-reduced-motion: reduce)`
- 전환 직후 **되돌리기 스낵바** 노출. 잘못 눌렀는데 애니메이션까지 봐야 하면 곤란하다
- 햅틱은 `navigator.vibrate()`. **iOS 사파리는 지원하지 않는다.** 없어도 연출이 성립해야 하므로 햅틱은 덤으로만 취급한다

---

## 8. 접근성

| 항목 | 기준 |
|---|---|
| 등급 전달 | 색 + 활자 배지 병기(§6.2). 색상 단독 금지 |
| 명암비 | 본문 4.5:1 이상. 카드리스트는 §2.3 팔레트, 랜딩은 점토 카드 실측 필수 |
| 터치 영역 | 최소 48×48px |
| 모션 | `prefers-reduced-motion` 존중 |
| 글자 크기 | 브라우저 확대 200%까지 레이아웃 유지. 카드는 높이 확장 허용 |
| 키보드 | 모든 조작이 `Tab`으로 닿아야 한다. 포커스 링은 다크 브론즈 2px |
| 시맨틱 | 누르는 것은 `<button>`, 이동하는 것은 `<a>`. `<div>`에 클릭 핸들러를 달지 않는다 |
| 확대 | `user-scalable=no` 금지. 확대를 막지 않는다 |

---

## 9. 공유 이미지 카드

서비스에서 **유일하게 외부로 나가는 산출물**이므로 화면 카드와 별도로 설계한다. 화면 카드의 3항목 제약이 여기서는 풀린다.

### 9.1 규격

- **1:1 정사각형**, 1080×1080px
- 배경은 석회암 판, 중앙에 해당 등급의 석재 카드
- 화면 카드보다 여백을 넉넉히

**웹 구현**: Canvas에 그린다. 화면 카드를 DOM에서 이미지로 변환하는 방식(html2canvas 등)은 쓰지 않는다 — 규격과 정보 구성이 화면 카드와 다르므로 어차피 별도 렌더링이고, 외부 라이브러리 없이 Canvas API만으로 충분하다.

- 논리 크기 1080×1080으로 고정 — 기기 화면 밀도와 무관하게 결과물이 같아야 한다
- 웹폰트가 로드된 뒤에 그린다. `document.fonts.ready`를 기다리지 않으면 폴백 서체로 렌더링된다
- 재질 텍스처는 SVG를 `Image`로 로드해 `drawImage` — CSS 배경은 Canvas에 직접 못 쓴다
- 결과는 `canvas.toBlob()` → `navigator.share({ files })`, 미지원 시 다운로드 (PRD §5.5)

### 9.2 내용

| 항목 | 표기 |
|---|---|
| 식당명 | 크게 |
| 등급 | 재질 + 활자 |
| 방문 횟수 | `47회 방문` |
| 누적 금액 | `300만원+` (구간 표시 기본) |
| 기간 | `2023년부터` |

메모와 메뉴 내역은 **절대 포함하지 않는다.** PRD상 항상 비공개다.

### 9.3 공유 단위

**식당 1곳 = 이미지 1장**이 기본이다. 실제 사용은 리스트 전체보다 한 곳 추천이 압도적으로 잦다.

리스트 공유 시에는 보낼 카드를 사용자가 고르게 하고 **최대 9장까지만** 허용한다. 12곳을 그대로 보내면 메신저가 도배된다.

### 9.4 워터마크

우하단, 서비스명(`나또감`)만 재색 작은 활자로. 로고나 QR은 넣지 않는다.

---

## 10. 미결정 항목

| # | 항목 | 비고 |
|---|---|---|
| 1 | 점토 카드 명암비 실측 | 미달 시 점토 톤 상향. **아직 미실측** |
| 2 | 석재 텍스처 제작 방법 | **해소됨(더는 필요 없음)** — SVG `feTurbulence` 코드 생성 프로토타입은 있었으나(에셋 0바이트), §0 개정으로 카드리스트에 이어 랜딩 쇼케이스 카드도 재질을 쓰지 않게 되면서 텍스처 자체가 코드에서 제거됨 |
| 3 | 숫자 전용 서체 도입 여부 | 산세리프 유지 전제에서 숫자만 다른 산세리프를 쓸지. 대비는 커지나 통일감은 떨어짐 |
| 4 | 다크 모드 | v1 제외. 도입 시 §2.3 배틀카드 팔레트(전 화면 공용) 하나만 재설계하면 된다 — 재질 팔레트가 사라지며 오히려 단순해짐 |
| 5 | 아이콘 세트 | 선 아이콘 계열 미정 (카드 캐릭터 일러스트는 §6.2b로 별도 확정, 카드리스트·랜딩 공용) |
| 6 | 등급 배지 표기 | 네 글자 전체(`인생맛집`) vs 줄임말(`인생`). §6.2 개정으로 배지가 카드 전체 폭 플레이트가 되면서 당장은 전체 표기로 충분해 보류 |
| 7 | 랜딩페이지와 서비스 화면의 표현 수위 | **좁혀짐.** 이전엔 "랜딩만 화려해도 된다"였으나, §0 확장 이후로는 랜딩·카드리스트·랭킹이 같은 배틀카드 팔레트를 쓴다. §1.2의 절제 원칙은 이제 화면 단위가 아니라 "인생맛집 홀로그램만 예외"라는 더 좁은 범위로 적용된다. 랜딩에 남은 유일한 차이는 3D 틸트/회전/광택 추적 상호작용(§7, 인터랙션 자체는 여전히 랜딩 전용) |
| 8 | 등급 전환 애니메이션(§7.2) | 계획만 있고 미구현. 재질 크로스페이드였던 원안을 카드 색 크로스페이드로 개정했으나 실제 구현은 아직 없음 |

---

## 부록 — 의사결정 기록

| # | 항목 | 결정 |
|---|---|---|
| 1 | 전체 인상 | ~~수집 도감. 광물 표본집~~ → 전 화면 배틀카드(§0 개정, 2026-08-24) |
| 2 | 등급 표현 | ~~카드 재질 차등~~ → §2.3 색 팔레트 차등. 카드리스트·랜딩·랭킹이 공유 |
| 3 | 기본 톤 | 라이트 |
| 4 | 재질 계열 | ~~석재 — 대리석/화강암/사암/점토~~ → **폐기.** 코드에서도 삭제됨(§3) |
| 5 | 배경 | 흰색 아님. ~~석회암 판(회색)~~ → **따뜻한 베이지**(`#E7DCC3`, 2026-08-24, 전역 UI 배경) |
| 6 | 강조색 | 무광 황동 + 다크 브론즈 (글자용, 전역 UI) |
| 7 | 서체 | 전부 산세리프. 굵기·크기 대비로 위계 |
| 8 | 숫자 | 등폭 숫자 필수 |
| 9 | 목록 배치 | ~~2열 격자, 정보 3항목~~ → 등급 구역별 유동 격자, 3:4 카드, 정보 4항목(식당명·캐릭터·등급 배지·방문누적 스탯) (§5.2) |
| 10 | 등급 전환 | 카드 색 크로스페이드 애니메이션(계획, 미구현). 카드리스트에서 상시로 도는 모션은 인생맛집 홀로그램(§6.2c)뿐 |
| 11 | 공유 카드 | 식당당 1장, 1:1, 최대 9장 |
| 12 | 플랫폼 | 웹. 치수 단위는 `px`, 모바일 브라우저 기준 |
| 13 | 랜딩페이지 | 쇼케이스로 절제 원칙 예외. 3D 카드·360° 회전·광택 추적은 유지(색 팔레트만 배틀카드로 개정) |
| 35 | 등급 팔레트 (2026-08-24, 카드리스트→전 화면 확장) | 재질 대신 등급별 색(골드/그린/블루/퍼플) 팔레트로 전환. 처음엔 카드리스트만, 이후 랜딩 3D 카드·랭킹 페이지까지 같은 정의를 공유하도록 확장. §2.3 |
| 36 | 카드리스트 캐릭터 (2026-08-24) | 우상단 이미지 칸을 등급별 돼지 마스코트 일러스트(배경 투명 PNG)로 채움. 랜딩 쇼케이스 카드도 같은 에셋 재사용. §6.2b |
| 37 | 인생맛집 홀로그램 (2026-08-24) | 오로라+프리즘 홀로(+카드리스트는 크리스탈 글레어까지) 상시 루프. §1.2 절제 원칙의 명시적 예외. §6.2c |
| 38 | 방문·누적 표시 (2026-08-24) | 정렬 연동 단일 숫자 계획을 폐기하고 항상 함께, 카드 하단 중앙에 능력치 형태로 표시. §5.3 |
| 39 | 카드 모서리 (2026-08-24) | 전 화면 10px 라운드로 통일(카드리스트·랜딩 모두). §5.2 |
| 40 | 배경색 (2026-08-24) | 석회암(회색, `#DAD5CC`) → 따뜻한 베이지(`#E7DCC3`)로 전 화면 개정. CSS 변수 `--limestone` → `--beige` |
| 41 | 랭킹 페이지 카드 (2026-08-24) | 포디움·리스트 항목에 등급 팔레트 적용(프레임·배경·pill 배지 색, 아바타 틴트). 순위 색(1위 브론즈 링)은 등급과 별개로 유지. §6.6 |
| 42 | 카드리스트 검색 (2026-08-24) | 이름으로 즉시 필터링하는 검색창 추가. 등급 구역 구조·정렬은 유지, 매치 없는 구역은 숨김. §6.5 |
