# DESIGN — 나또감 (가제)

> 석재 표본집. 사진 없이 재질과 숫자로만 짓는 화면.

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-08-14 |
| 관련 문서 | PRD.md |
| 플랫폼 | 웹 우선(랜딩 → 웹앱) → Android. 라이트 모드 기본 |

---

## 1. 디자인 원칙

### 1.1 전제

PRD가 이미 디자인을 강하게 규정한다.

- **사진이 없다.** 화면을 채우는 것은 재질, 숫자, 글자뿐이다.
- **첫 화면이 통계다.** 숫자 가독성이 최우선이다.
- **대표 숫자는 인생맛집 개수다.** 이 앱은 가계부가 아니라 컬렉션이다.

### 1.2 원칙

| # | 원칙 | 의미 |
|---|---|---|
| 1 | 구조는 도감, 표현은 절제 | 카드와 격자라는 수집의 뼈대는 쓰되, 장식과 애니메이션은 최소화한다. 포켓몬 도감이 아니라 광물 표본집이다. |
| 2 | 재질은 느껴지되 보이지 않는다 | 질감이 눈에 띄면 실패다. 알아채기 직전까지만. |
| 3 | 보상은 한 곳에만 | 앱에서 큰 모션은 등급 전환 하나뿐이다. |
| 4 | 재질은 항상 활자와 함께 | 질감만으로 등급을 전달하지 않는다. |
| 5 | 강등은 축하하지 않는다 | 승격과 강등의 연출은 대칭이 아니다. |

### 1.3 시그니처

**등급 변경 시 카드 표면의 재질이 실제로 바뀌는 전환.**

이 앱에서 기억에 남을 단 하나의 순간이다. 재질이 장식이 아니라 상태 그 자체임을 증명하는 지점이며, 여기에 모든 모션 예산을 쓴다.

---

## 2. 색

### 2.1 팔레트

바탕은 흰색이 아니다. **석회암 판** 위에 표본을 올려놓은 구조이며, 배경이 흰색이면 최고 등급인 대리석 카드가 배경에 녹아 사라진다.

| 이름 | HEX | 용도 |
|---|---|---|
| 석회암 판 | `#DAD5CC` | 배경 |
| 먹 | `#2A2724` | 본문, 식당명, 숫자 |
| 재 | `#6E675E` | 보조 정보, 라벨 |
| 무광 황동 | `#A8874E` | 강조 면, 아이콘, 기록 버튼 |
| 다크 브론즈 | `#6B5327` | 강조 글자 |

### 2.2 황동 사용 규칙

금색은 가장 쉽게 촌스러워지는 색이다. 다음을 엄격히 지킨다.

- **화면당 황동은 한두 곳까지.** 남발하면 위계가 죽는다.
- 그라디언트 금색 금지. 무광 단색만.
- 반짝임, 광택, 메탈릭 효과, 금색 테두리 금지.
- 황동은 **면과 아이콘**에만. 글자에 색이 필요하면 다크 브론즈를 쓴다. 밝은 배경에서 황동 글자는 대비가 부족하다.

---

## 3. 재질 시스템

### 3.1 등급별 재질

위로 갈수록 결이 정교해지고, 아래로 갈수록 배경에 가라앉는다. 재질의 영속성이 등급의 위계다.

| 등급 | 재질 | 표면색 | 결 | 결 색 | 그림자 |
|---|---|---|---|---|---|
| 인생맛집 | 대리석 | `#FAF8F5` | 가늘고 유려한 맥 | `#CFC8BC` | 깊음 (y+6, blur 16, 12%) |
| 맛집 | 화강암 | `#EBE7E0` | 촘촘한 미세 반점 | `#B4AEA4` | 중간 (y+3, blur 8, 10%) |
| 무난 | 사암 | `#DFD8CA` | 거칠고 균일한 입자 | `#BCB0A0` | 얕음 (y+1, blur 4, 8%) |
| 비추 | 점토 | `#D3CEC6` | 결 없음 | — | 없음 |
| 미지정 | 무지 | `#D8D3CA` | 없음, 점선 테두리 | — | 없음 |

점토(`#D3CEC6`)는 배경(`#DAD5CC`)보다 미세하게 어둡다. 떠오르지 않고 가라앉는다.

### 3.2 구현 규칙

- 텍스처는 **등급당 이미지 1장씩만** 만들어 전 카드가 재사용한다. 카드마다 생성하면 목록 스크롤이 끊긴다.
- 노이즈 강도는 알파 8% 이하. 원본 표면색과의 명도차 5% 이내.
- 다크 모드는 v1 범위 밖. 지원 시 석재 팔레트 전체를 재설계해야 하며, 단순 반전은 불가능하다.

### 3.3 대비 검증 (필수)

점토 카드 위 먹색 글자가 가장 위험하다. 구현 시 실측하고 WCAG AA(4.5:1) 미달이면 **점토 톤을 밝히는 쪽으로** 조정한다. 글자를 어둡게 하는 방향은 이미 한계다.

---

## 4. 타이포그래피

### 4.1 서체

전부 산세리프. Pretendard 단일 계열을 쓰되, **굵기와 크기의 대비를 극단적으로 벌려** 위계를 만든다. 세리프를 쓰지 않기로 한 이상 도록 같은 무게는 서체가 아니라 여백·재질·황동이 짊어진다.

### 4.2 역할

| 역할 | 굵기 | 크기 | 자간 | 용도 |
|---|---|---|---|---|
| 대표 숫자 | ExtraBold | 72 | -3% | 대시보드 인생맛집 개수 |
| 지표 숫자 | Bold | 28 | -1% | 누적 금액, 방문 횟수 |
| 식당명 | SemiBold | 16 | 0 | 카드, 목록 |
| 본문 | Regular | 15 | 0 | 메모, 설명 |
| 라벨 | Medium | 12 | +2% | 보조 정보, 단위 |

### 4.3 숫자 규칙

**등폭 숫자(tabular figures)를 반드시 켠다.** 금액과 횟수가 세로로 나열되는 화면이 많은데, 비례 숫자를 쓰면 자릿수가 어긋나 지저분해 보인다.

```
font-feature-settings: "tnum" 1;
```

- 금액은 만원 단위 축약: `380만원`, `1,240만원`
- 방문 횟수는 항상 `회` 단위 병기
- 단위는 숫자보다 한 단계 작고 재색

---

## 5. 레이아웃

### 5.1 간격

4의 배수. 기본 단위 4dp.

| 토큰 | 값 | 용도 |
|---|---|---|
| xs | 4 | 활자 간 |
| sm | 8 | 카드 내부 요소 간 |
| md | 16 | 카드 내부 여백, 격자 간격 |
| lg | 24 | 화면 좌우 여백 |
| xl | 40 | 섹션 구분 |

### 5.2 식당 격자

**2열 격자.** 진열장 구조.

- 카드 비율 **3:4** (세로로 긴 직사각형). 정사각형보다 표본 카드에 가깝고, 긴 한글 식당명을 두 줄로 받는다.
- 모서리 반경 4dp. 석재는 둥글지 않다.

카드에는 **세 가지만** 넣는다.

```
┌──────────────┐
│      인생맛집│  ← 등급 활자 배지 (우상단)
│              │
│              │
│ 을지로       │  ← 식당명 최대 2줄, 말줄임
│ 골뱅이       │
│              │
│ 47회         │  ← 정렬 기준에 따라 바뀌는 숫자
└──────────────┘
```

### 5.3 정렬과 표시 숫자의 연동

칸에 숫자를 하나만 넣는 문제는 **정렬 기준과 연동해서** 푼다.

| 정렬 기준 | 카드에 표시되는 숫자 |
|---|---|
| 방문 횟수순 | `47회` |
| 누적 금액순 | `380만원` |
| 최근 방문순 | `3일 전` |
| 등급순 | `47회` (기본값) |

지금 무엇을 기준으로 보고 있는지가 모든 칸에 반복되므로 헷갈리지 않는다. 나머지 정보는 상세 화면에 있다.

---

## 6. 컴포넌트

### 6.1 대시보드 대표 숫자

화면 상단, 좌측 정렬. 가운데 정렬하지 않는다 — 가운데 정렬한 큰 숫자와 작은 라벨은 어떤 앱에나 있는 기본값이다.

```
인생맛집
8 / 9              ← 72pt ExtraBold, 우측 정원은 재색 절반 크기
멀리서 일부러라도 또 가는 곳
```

정원 대비 표기(`8 / 9`)가 희소성을 상시 노출한다. PRD상 v1은 초과를 차단하지 않으므로 초과 시에는 `11 / 9`로 표시하고 숫자를 다크 브론즈로 물들인다. 경고문은 띄우지 않는다. 숫자가 이미 말하고 있다.

### 6.2 등급 배지

등급 이름이 한 글자 알파벳에서 한글 두~네 글자로 바뀌면서 정사각 규격이 성립하지 않는다. 가변폭 활자로 다시 정의한다.

- 카드 우상단, **우측 정렬 가변폭**. 높이 기준 12dp
- 활자만. 도형 배경 없음
- 11pt Medium. 식당명(16pt SemiBold)보다 확실히 작아야 두 활자가 경쟁하지 않는다
- 식당명 영역과 최소 8dp를 띄운다
- **인생맛집만 다크 브론즈**, 나머지는 재색

> 네 글자(`인생맛집`)가 좁은 카드에서 식당명과 경쟁하면 줄임말(`인생` / `맛집` / `무난` / `비추`)로 전환한다. §10 미결정 참조.

### 6.3 기록 버튼

- 전역 고정, 우하단
- 무광 황동 원형, 지름 56dp
- 아이콘은 흰색 선 아이콘 하나
- **화면당 황동 한두 곳 규칙에 따라, 기록 버튼이 있는 화면에서는 다른 황동 요소를 쓰지 않는다.**

### 6.4 빈 상태

PRD의 단계적 공개를 따른다. 빈 화면은 사과하지 않고 행동을 지시한다.

| 기록 수 | 문구 |
|---|---|
| 0건 | 첫 기록을 남기세요 / 세 곳을 기록하면 통계가 열립니다 |
| 1~2건 | 두 곳 더 기록하면 통계가 열립니다 |

---

## 7. 모션

### 7.1 원칙

앱에서 큰 모션은 **등급 전환 하나뿐**이다. 나머지 화면 전환은 표준 페이드, 200ms 이하. 여기저기 움직이면 아무것도 특별하지 않다.

### 7.2 등급 전환

| 방향 | 시간 | 이징 | 부가 |
|---|---|---|---|
| 승격 | 500ms | ease-out | 햅틱 1회, 그림자 심화 동반 |
| 강등 | 250ms | linear | 햅틱 없음, 소리 없음 |

- 구현은 **텍스처 크로스페이드**. 파티클, 광채, 스케일 튐 없음
- 승격은 결이 조밀해지며 카드가 떠오르고, 강등은 같은 전환을 되감으며 가라앉는다
- 시스템 모션 줄이기 설정이 켜져 있으면 **즉시 전환**
- 전환 직후 **되돌리기 스낵바** 노출. 잘못 눌렀는데 애니메이션까지 봐야 하면 곤란하다

---

## 8. 접근성

| 항목 | 기준 |
|---|---|
| 등급 전달 | 재질 + 활자 배지 병기. 질감 단독 금지 |
| 명암비 | 본문 4.5:1 이상. 점토 카드 실측 필수 |
| 터치 영역 | 최소 48×48dp |
| 모션 | 시스템 설정 존중 |
| 글자 크기 | 시스템 배율 200%까지 레이아웃 유지. 카드는 높이 확장 허용 |

---

## 9. 공유 이미지 카드

앱에서 **유일하게 외부로 나가는 산출물**이므로 앱 화면 카드와 별도로 설계한다. 화면 카드의 3항목 제약이 여기서는 풀린다.

### 9.1 규격

- **1:1 정사각형**, 1080×1080px
- 배경은 석회암 판, 중앙에 해당 등급의 석재 카드
- 앱 화면보다 여백을 넉넉히

### 9.2 내용

| 항목 | 표기 |
|---|---|
| 식당명 | 크게 |
| 등급 | 재질 + 활자 |
| 방문 횟수 | `47회 방문` |
| 누적 금액 | `300만원+` (구간 표시 기본) |
| 기간 | `2023년부터` |

메모와 메뉴 내역은 **절대 포함하지 않는다.** PRD상 항상 비공개다.

### 9.3 공유 단위

**식당 1곳 = 이미지 1장**이 기본이다. 실제 사용은 리스트 전체보다 한 곳 추천이 압도적으로 잦다.

리스트 공유 시에는 보낼 카드를 사용자가 고르게 하고 **최대 9장까지만** 허용한다. 12곳을 그대로 보내면 메신저가 도배된다.

### 9.4 워터마크

우하단, 앱 이름만 재색 작은 활자로. 로고나 QR은 넣지 않는다.

---

## 10. 미결정 항목

| # | 항목 | 비고 |
|---|---|---|
| 1 | 점토 카드 명암비 실측 | 미달 시 점토 톤 상향. **아직 미실측** |
| 2 | 석재 텍스처 제작 방법 | 웹 랜딩은 SVG `feTurbulence` 코드 생성으로 프로토타입 완료(에셋 0바이트). 앱에서도 같은 방식을 쓸지, 강도를 얼마로 둘지 미정 |
| 3 | 숫자 전용 서체 도입 여부 | 산세리프 유지 전제에서 숫자만 다른 산세리프를 쓸지. 대비는 커지나 통일감은 떨어짐 |
| 4 | 다크 모드 | v1 제외. 도입 시 석재 팔레트 전면 재설계 필요 |
| 5 | 아이콘 세트 | 선 아이콘 계열 미정 |
| 6 | 등급 배지 표기 | 네 글자 전체(`인생맛집`) vs 줄임말(`인생`). 좁은 카드에서 식당명과 경쟁하는지 확인 후 결정 (§6.2) |
| 7 | 랜딩페이지와 앱의 표현 수위 | 랜딩은 쇼케이스로 3D 카드·360° 회전·광택 추적을 쓴다. 앱 내부는 §1.2 절제 원칙 유지가 현재 방침. §9 공유 이미지처럼 **외부로 나가는 표면은 제약이 풀린다**는 선례를 따른 것 |

---

## 부록 — 의사결정 기록

| # | 항목 | 결정 |
|---|---|---|
| 1 | 전체 인상 | 수집 도감. 단 광물 표본집 방향, 포켓몬 도감 아님 |
| 2 | 등급 표현 | 카드 재질 차등 |
| 3 | 기본 톤 | 라이트 |
| 4 | 재질 계열 | 석재 — 대리석/화강암/사암/점토 |
| 5 | 배경 | 흰색 아님. 석회암 판 |
| 6 | 강조색 | 무광 황동 + 다크 브론즈 (글자용) |
| 7 | 서체 | 전부 산세리프. 굵기·크기 대비로 위계 |
| 8 | 숫자 | 등폭 숫자 필수 |
| 9 | 목록 배치 | 2열 격자, 3:4 카드, 정보 3항목 |
| 10 | 등급 전환 | 재질 크로스페이드 애니메이션. 앱의 유일한 큰 모션 |
| 11 | 공유 카드 | 식당당 1장, 1:1, 최대 9장 |
