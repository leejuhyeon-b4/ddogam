# PRD — 미식 장부 (가칭)

> 방문 횟수와 누적 금액이 증명하는, 나만의 맛집 등급 서비스

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-08-14 |
| 성격 | 개인 프로젝트 (본인 사용 목적) |
| 대상 플랫폼 | Android (우선) |
| 관련 문서 | DESIGN.md (별도 작성 예정) |

---

## 1. 배경과 문제 정의

### 1.1 기존 맛집 리뷰 서비스의 한계

시중의 리뷰 서비스는 **한 번 간 사람의 인상**을 모은다. 별점 5개는 광고일 수도, 첫 방문의 흥분일 수도 있다. 리뷰가 많을수록 신뢰도가 올라가는 구조지만, 정작 "이 사람이 이 집을 진짜 좋아하는가"는 알 수 없다.

### 1.2 이 서비스의 전제

**돈과 발걸음은 거짓말을 하지 않는다.**

어떤 사람이 3년간 47번 방문하고 380만원을 쓴 식당이 있다면, 그 사실 하나가 어떤 장문의 후기보다 강력하다. 이 서비스는 글이 아니라 **증거**를 쌓는다.

### 1.3 핵심 차별점

| # | 차별점 | 설명 |
|---|---|---|
| 1 | 방문 횟수 누적 | 식당별로 몇 번 갔는지 자동 집계 |
| 2 | 누적 지출액 | 그 식당에서 지금까지 총 얼마를 썼는지 |
| 3 | 나만의 등급 | S/A/B/C 4단계 개인 등급 체계 |
| 4 | S등급 희소성 | S는 정원제로 관리되어 남발할 수 없음 |

---

## 2. 목표와 성공 지표

### 2.1 목표

내가 6개월 뒤에도 계속 쓰는 앱을 만든다. 사용자 수, 수익, 성장은 목표가 아니다.

### 2.2 성공 지표

| 지표 | 목표 | 측정 방법 |
|---|---|---|
| 지속 사용 | 출시 6개월 후에도 주 1회 이상 기록 | 본인 사용 로그 |
| 기록 소요 시간 | 수동 기록 기준 10초 이내 | 직접 측정 |
| 영수증 없는 기록 비율 | 전체의 40% 이상 | 수동 입력이 폴백이 아닌 정식 경로로 작동하는지 확인 |
| OCR 성공률 | 스캔 시도 중 70% 이상이 수정 없이 통과 | 수정 발생 횟수 집계 |

### 2.3 비목표 (Non-goals)

- 다른 사용자의 리뷰를 읽는 기능
- 식당 추천 / 검색 랭킹
- 팔로우, 피드, 댓글 등 소셜 기능
- 수익화

---

## 3. 핵심 개념

### 3.1 등급 정의

등급은 형용사가 아니라 **행동**으로 정의한다. "맛있다/괜찮다"로 정의하면 시간이 지나며 전부 상위 등급으로 몰리기 때문이다.

| 등급 | 정의 |
|---|---|
| **S** | 멀리서 일부러라도 찾아간다 |
| **A** | 근처에 가면 무조건 들른다 |
| **B** | 누가 가자고 하면 간다 |
| **C** | 다시 가지 않는다 |

### 3.2 등급의 단위

- 등급은 **식당당 하나**만 존재한다.
- 방문 기록에는 등급이 없다. 매번 점수를 매기는 부담을 없애기 위함이다.
- 등급은 사용자가 원할 때만 수동으로 변경한다.
- 등급 변경 이력은 로그로 보관한다. ("2년째 S 유지", "작년 A→B 강등" 같은 서사 확보)

### 3.3 S등급 정원제

S가 무제한이면 대시보드 대표 숫자(S등급 수)를 키우기 위해 등급을 남발하게 된다. 정원을 두면 11번째 후보가 나타났을 때 기존 하나를 내려야 하고, 그 순간이 이 앱에서 가장 진지한 의사결정이 된다.

정원은 기록한 식당 수에 비례해 **계단식**으로 확장한다. 선형 비율은 초반이 너무 가혹하고(첫 S를 얻으려 10곳 필요), 고정 정원은 초반에 무의미하다(3곳 기록했는데 슬롯 10개).

| 기록 식당 수 | S 정원 |
|---|---|
| 1 ~ 4 | 1 |
| 5 ~ 14 | 2 |
| 15 ~ 29 | 3 |
| 30 ~ 49 | 4 |
| 50 이상 | 이후 25곳마다 +1 |

정원은 **S에만 적용**한다. A·B·C는 개수 제한이 없다. 전 등급 강제 분포는 기록할 때마다 순위 조정이 따라붙어, 기록을 가볍게 유지한다는 원칙과 충돌한다.

> **v1 적용 범위**: 계산 로직은 구현하되 **차단하지 않는다.** 권장 개수만 표시하고 초과를 허용한다. v2에서 스위치를 켜 강제 전환한다. (사후 도입 시 강등 마이그레이션이 필요해지는 것을 방지)

---

## 4. 데이터 모델

### 4.1 엔티티

```
Restaurant (식당)
├─ id
├─ name                  표시용 이름 (사용자 확인/수정 가능)
├─ business_number       사업자등록번호 (영수증 OCR에서 추출, 고유키)
├─ kakao_place_id        카카오 장소 ID (선택)
├─ address
├─ grade                 S / A / B / C / 미지정
├─ created_at
└─ [파생] visit_count, total_spent, first_visit_at, last_visit_at

Visit (방문)
├─ id
├─ restaurant_id
├─ visited_at            날짜 (+시각)
├─ amount                금액
├─ memo                  한줄 메모
├─ receipt_image_path    영수증 원본 (보관 여부는 §9 미결정 항목)
└─ source                manual / ocr

MenuItem (메뉴 항목)          ※ v1은 저장만, UI 미노출
├─ id
├─ visit_id
├─ name
├─ quantity
└─ price

GradeChange (등급 변경 이력)
├─ id
├─ restaurant_id
├─ from_grade
├─ to_grade
└─ changed_at

List (리스트)
├─ id
├─ name
├─ is_auto               S등급 리스트 등 자동 생성 여부
└─ restaurant_ids[]
```

### 4.2 설계 원칙

- **방문 횟수와 누적 금액은 저장하지 않고 Visit에서 집계한다.** 중복 저장 시 불일치가 발생한다.
- **사업자등록번호가 식당의 진짜 고유키다.** 영수증에서 추출되며, 상호명 변경이나 지도 데이터 변동에 영향받지 않는다.
- **카카오 장소 ID는 보조 식별자다.** 카카오 로컬 API는 사업자등록번호를 제공하지 않으므로 둘은 자동 연결되지 않는다. 첫 방문 시 사용자에게 1회 확인시켜 매핑을 저장하고, 이후에는 영수증만으로 자동 연결한다.

---

## 5. 기능 명세

### 5.1 방문 기록 (핵심)

**설계 원칙: 수동 입력이 폴백이 아니라 정식 경로다.**

영수증은 자주 안 받거나 버려진다. OCR을 주 경로로 설계하면 영수증 없는 날 사용자가 이탈한다. OCR은 폼을 미리 채워주는 보조 수단일 뿐이다.

#### 5.1.1 수동 기록

| 필드 | 필수 | 비고 |
|---|---|---|
| 식당 | O | 검색 또는 직접 입력 |
| 날짜 | O | 기본값 = 오늘 |
| 금액 | O | 숫자 키패드 즉시 노출 |
| 한줄 메모 | X | |

- 목표 소요 시간 **10초 이내**
- 기록 버튼은 모든 화면에서 접근 가능한 전역 고정 요소

#### 5.1.2 영수증 스캔

- 카메라 촬영 또는 갤러리 선택
- Android 공유 시트로 스크린샷 수신 (키오스크 화면, 배달앱 주문 상세, 간편결제 완료 화면)
- OCR 결과는 **기록 폼을 미리 채운 상태**로 표시되며, 사용자가 검토 후 저장
- 추출 대상: 사업자등록번호, 상호명, 결제일시, 총액, 메뉴 항목별 내역

> OCR은 반드시 틀린다. 수정 UI가 매끄럽지 않으면 스캔 기능 자체가 부담이 된다.

#### 5.1.3 식당 선택

1. 카카오 로컬 API 키워드 검색 (1차)
2. 검색 결과에 없으면 **직접 입력** (폴백, 필수 기능)

> 카카오맵에서는 검색되는데 API로는 안 잡히는 사업장이 존재한다. 검색 실패 시 기록을 포기하게 만들면 안 된다.

### 5.2 등급 관리

- 식당 상세 화면에서 S/A/B/C 지정 및 변경
- 변경 시 GradeChange 로그 기록
- S 지정 시 현재 정원 대비 개수 표시 (v1은 안내만, 차단 없음)

### 5.3 대시보드 (홈)

앱을 열면 가장 먼저 보이는 화면.

**대표 숫자: S등급 식당 수**

금액을 대표 숫자로 두면 이 앱은 죄책감 앱이 된다. S등급 개수는 같은 데이터를 자부심 지표로 바꾼다. 앱의 성격이 장부에서 컬렉션으로 이동한다.

**보조 지표**
- 누적 총 지출액
- 총 방문 횟수
- 등록 식당 수 / 등급별 분포
- 최다 방문 식당 TOP 5
- 최다 지출 식당 TOP 5

**빈 상태 처리 — 단계적 공개**

신규 상태에서 0원·0회를 보여주는 것은 통계 앱의 최악의 첫인상이다. 기록 수에 따라 카드를 하나씩 연다.

| 기록 수 | 노출 |
|---|---|
| 0건 | 큰 기록 버튼 + "3번 기록하면 통계가 열립니다" |
| 3건~ | 대표 숫자, 누적 금액, 방문 횟수 |
| 10건~ | 등급 분포, TOP 5 |

### 5.4 식당 목록

- 등급순 / 방문 횟수순 / 누적 금액순 / 최근 방문순 정렬
- 식당 상세: 등급, 방문 횟수, 누적 금액, 방문 이력 타임라인, 등급 변경 이력

### 5.5 리스트와 공유

- 사용자가 임의로 리스트 생성 ("을지로 술집", "부모님 모시고 갈 곳")
- **S등급 리스트는 자동 생성**되어 별도 관리 불필요

#### 공유 방식: 이미지 카드 내보내기

서버가 없으므로 링크 공유는 불가능하다. 대신 리스트를 한 장의 이미지로 렌더링해 시스템 공유 시트로 내보낸다. 운영비가 들지 않고, 오히려 메신저에서 더 잘 유통된다.

**이미지에 포함되는 정보**

| 항목 | 포함 |
|---|---|
| 식당명 | O |
| 등급 | O |
| 방문 횟수 | O |
| 누적 금액 | O (기본은 구간 표시) |
| 한줄 메모 | X (항상 비공개) |
| 메뉴 내역 | X (항상 비공개) |

> **금액 구간 표시**: 기본값은 "300만원+" 형태. 정확한 금액 노출은 사용자가 명시적으로 켜는 옵션. 신뢰 신호는 유지하면서 소비 수준 추정을 어렵게 한다.

이 서비스의 공유물은 **글이 아니라 증거**다. 후기를 읽히는 것이 아니라, 실제로 갔다는 사실을 보여준다.

### 5.6 백업과 복원

- 전체 데이터를 단일 파일로 내보내기 (JSON 또는 ZIP)
- 파일에서 복원
- 기기 변경 및 데이터 유실 대비의 **유일한 수단**이므로 반드시 v1에 포함

---

## 6. 화면 구조

```
홈 (대시보드)
├─ 대표 숫자: S등급 수
├─ 보조 지표 카드
└─ [전역] 기록 버튼

식당 목록
└─ 식당 상세
    ├─ 등급 변경
    ├─ 방문 이력
    └─ 등급 변경 이력

리스트
├─ S등급 (자동)
├─ 사용자 리스트
└─ 이미지로 내보내기

기록 (모달)
├─ 수동 입력
└─ 영수증 스캔

설정
├─ 백업 / 복원
└─ 금액 표기 방식
```

---

## 7. 기술 방향

| 영역 | 선택 | 비고 |
|---|---|---|
| 플랫폼 | Android | |
| 데이터 저장 | 로컬 DB | 서버 없음, 운영비 0 |
| 백업 | 파일 내보내기/가져오기 | |
| OCR 1단계 | ML Kit 온디바이스 텍스트 인식 | 무료, 오프라인. **이미지는 기기 밖으로 나가지 않음** |
| OCR 2단계 | Gemini Flash (무료 티어) | 추출된 **텍스트만** 전송하여 JSON 구조화 |
| API 키 | 사용자가 설정 화면에서 직접 입력 (BYOK) | 서버가 없어 키를 앱에 내장할 수 없음 |
| 식당 검색 | 카카오 로컬 API | 무료. 사업자등록 필요 여부 확인 요망 |
| 계정 | 없음 | |

### 7.0 OCR 파이프라인

```
영수증 이미지
   ↓  ML Kit (온디바이스)
원시 텍스트
   ↓  민감 패턴 마스킹 (카드번호 등)
마스킹된 텍스트
   ↓  Gemini Flash (네트워크)
구조화 JSON { 사업자등록번호, 상호명, 일시, 총액, 메뉴[] }
   ↓
기록 폼 프리필 → 사용자 검토 → 저장
```

이미지 대신 텍스트를 보내는 이유:
- 영수증 원본이 외부로 나가지 않아 프라이버시 노출이 크게 줄어듦
- 토큰 소비가 적어 무료 한도를 오래 씀
- 업로드가 없어 응답이 빠름

한국어 영수증은 포맷이 제각각이라 규칙 기반 파싱이 어렵다. 그 부분만 LLM에 위임하고 시각 인식은 온디바이스가 담당하는 역할 분담이다.

### 7.1 외부 의존성 주의사항

**카카오 로컬 API**
- 응답에 사업자등록번호 없음 → 영수증 데이터와 자동 연결 불가 (§4.2 참조)
- 카카오맵에서 검색되는 장소가 API에서 누락되는 사례 존재
- 비즈앱 심사에 사업자등록증이 요구될 수 있음 → **개인 프로젝트의 최대 리스크**
- 검색 결과 대량 저장은 이용정책 위반 소지. place_id 중심으로 저장하고 표시 정보는 자체 레코드로 관리

**Gemini API 무료 티어**
- **무료 티어는 프롬프트가 구글 모델 학습에 사용될 수 있다.** 이미지 대신 마스킹된 텍스트만 전송하는 것이 그 대응책이다.
- 한도 수치는 자료마다 하루 20건에서 1,500건까지 편차가 크다. 공식 rate limits 페이지에서 직접 확인할 것.
- 2025년 12월 무료 한도가 급격히 축소된 전례가 있다. **한도가 0이 되어도 앱이 작동해야 한다.** 수동 입력이 정식 경로이므로 이 조건은 충족된다.
- 429 응답 시 즉시 재시도하지 말고 지수 백오프를 적용한다. 실패하면 조용히 수동 입력 폼으로 넘긴다.

**비용 원칙**
- 이 프로젝트는 **월 고정비 0원**을 전제로 한다. 유료 API는 도입하지 않는다.
- 유료 전환이 필요한 시점이 오면 기능을 줄이거나 수동 입력으로 후퇴한다.

---

## 8. 범위

### v1 (MVP)

- [x] 수동 방문 기록
- [x] 영수증 / 스크린샷 OCR 스캔 (ML Kit → Gemini Flash)
- [x] Gemini API 키 입력 설정 (BYOK)
- [x] 카카오 로컬 API 식당 검색 + 직접 입력 폴백
- [x] S/A/B/C 등급 부여 및 변경 이력
- [x] S 정원 계산 및 권장 개수 표시 (차단 없음)
- [x] 대시보드 (단계적 공개)
- [x] 식당 목록 및 상세
- [x] 리스트 생성 및 이미지 내보내기
- [x] 백업 / 복원
- [x] 메뉴 항목 데이터 **저장** (UI 미노출)

### v2

- [ ] S 정원 강제 적용
- [ ] 메뉴 항목별 내역 UI 및 통계 ("최다 주문 메뉴", "평균 객단가")
- [ ] 결제 알림 자동 감지 (Android 알림 접근 권한)
- [ ] 위치 기반 "내 주변 내 기록"

### 제외

- 사진 첨부
- 동행자 기록
- 소셜 기능 전반
- iOS

---

## 9. 미결정 항목

| # | 항목 | 비고 |
|---|---|---|
| 1 | 영수증 원본 이미지 보관 여부 | 사진을 안 쓰기로 했으나 스캔 시 이미지는 자동 생성됨. 저장 용량 vs 나중에 재확인 가능성 |
| 2 | 카카오 비즈앱 심사 통과 가능 여부 | 불가 시 공공데이터 인허가 정보 기반 자체 DB로 전환 |
| 3 | S 정원 계단식 수치 | 실사용 후 튜닝 |
| 4 | 서비스 이름 | |
| 5 | 등급 미지정 식당의 처리 | 기록만 하고 등급을 안 준 식당을 목록에서 어떻게 다룰지 |

---

## 10. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| 카카오 API 사용 불가 (사업자등록) | 높음 | 공공데이터 자체 DB + 직접 입력 주 경로화 |
| OCR 인식률 저조 | 중간 | 수동 입력이 이미 정식 경로. 수정 UI 품질에 투자 |
| Gemini 무료 한도 축소/폐지 | 중간 | 스캔 기능만 비활성화되고 수동 입력은 유지. 전례가 있으므로 대비 필수 |
| 영수증 데이터의 외부 학습 이용 | 중간 | 이미지 미전송, 민감 패턴 마스킹. 잔여 위험은 개인 사용 범위에서 감수 |
| 등급 인플레이션 | 중간 | 행동 기반 등급 정의 + v2 정원 강제 |
| 로컬 저장 데이터 유실 | 높음 | 백업 기능 v1 필수. 주기적 백업 리마인더 검토 |
| 기록 습관 미형성 | 높음 | 10초 기록 목표. 진입 마찰 최소화가 최우선 |

---

## 부록 A — 의사결정 기록

| # | 항목 | 결정 |
|---|---|---|
| 1 | 서비스 성격 | 개인 기록 기본, 선택적 공개 |
| 2 | 입력 방식 | 수동 입력이 정식 경로 + OCR 보조 |
| 3 | 식당 식별키 | 사업자등록번호 |
| 4 | 등급 부여 | 사용자 직접, 행동 기준 정의 |
| 5 | 등급 단위 | 식당당 1개, 방문에는 등급 없음 |
| 6 | 등급 이력 | 변경 로그 보관 |
| 7 | 방문 기록 필드 | 날짜·금액·메뉴·한줄 메모 |
| 8 | 사진·동행자 | 제외 |
| 9 | 식당 검색 | 카카오 로컬 API + 직접 입력 폴백 |
| 10 | 장소 저장 | place_id 중심 |
| 11 | 플랫폼 | Android 우선 |
| 12 | OCR 구조 | ML Kit 텍스트 추출 → Gemini Flash 구조화 |
| 12-1 | 비용 제약 | 월 고정비 0원. 유료 API 미사용 |
| 12-2 | API 키 | 사용자 직접 입력 (BYOK) |
| 13 | 홈 화면 | 누적 통계 대시보드 |
| 14 | 빈 상태 | 단계적 공개 |
| 15 | 기록 진입 | 전역 고정 버튼 |
| 16 | 대표 숫자 | S등급 식당 수 |
| 17 | 보조 지표 | 누적 금액, 방문 횟수 |
| 18 | S 정원 | 기록 수 비례 계단식 |
| 19 | 정원 범위 | S등급만 |
| 20 | 공유 단위 | 리스트 단위 |
| 21 | 공유 방식 | 이미지 카드 내보내기 |
| 22 | 공유 정보 | 식당명·등급·방문 횟수·누적 금액 |
| 23 | 금액 표기 | 구간 표시 기본 |
| 24 | 비공개 고정 | 메모, 메뉴 내역 |
| 25 | v1 제외 | S 정원 강제, 메뉴 UI |
| 26 | 숨은 수집 | 메뉴 데이터는 v1부터 저장 |
| 27 | 목표 | 개인 프로젝트 |
| 28 | 저장 | 로컬 + 파일 백업 |
